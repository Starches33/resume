spring:
  profiles:
    active: test
  datasource:
    url: jdbc:postgresql://92.63.178.149:5432/resume
    driver-class-name: org.postgresql.Driver
    username: resume
    password: resume
  jpa:
    show-sql: true
  liquibase:
    enabled: false

logging:
  level:
    org:
      springframework:
        cache: debug